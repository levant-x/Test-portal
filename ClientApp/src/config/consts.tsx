export const NAVBAR_HEIGHT_FACTOR = 5

export const LIKE_ACTIVE_CLASS_NAME = 'icon-warning'
export const TOKEN_BEARER_ID = '__token'

export enum APIEndpoints {
  articles = '/api/articles/all',
  userProfile = '/auth/profile',
}