export enum Paths {
  home = '/',
  profile = '/profile',
  logout = '/logout'
}

export enum Estimation {
  liked, disliked,
}

export interface IChildren {
  children?: React.ReactNode
}

